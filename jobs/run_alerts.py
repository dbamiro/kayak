"""Evaluate saved alerts against latest snapshots (placeholder for production)."""

from __future__ import annotations


def main() -> None:
    print(
        "run_alerts: placeholder — wire listing change detection + notifications "
        "(email/push) against `alerts.criteria` JSON."
    )


if __name__ == "__main__":
    main()
